# PLAN v2 — Slice m13.5h: Recruit e2e revival + mechanical dev_reducers fixme tripwire
(Working plan — deleted before PR. v2 after reviewer REJECT→amended + red-team findings.)

## Strategy per test (final)
- R1 REVIVE (gameplay): walk to grass ((1,2)↔(2,2) shuttle via __game().step, only while NOT in battle — red-team F6: queued moves drain during battle but encounter trigger is suppressed by the Ongoing guard; never step while Ongoing). Bounded MAX grass entries (P(no encounter in 40 entries)=0.8^40≈1.3e-4). On encounter: assert [data-testid="recruit-action"] visible + ongoingBattle.outcome==='Ongoing'; then Flee (text 'Flee'; flee costs nothing — no counterattack, battle GC'd).
- R2 REVIVE (gameplay, weaken-first — red-team F1/F7 kill the old 6-clicks design): per encounter, read the Opponent card DOM (`Opponent: <species>` / `Lv<k>` / `HP x/y · <affinity>`). If affinity==='Water' (SE vs Fire starter; Tidalin L7 max-roll can OHKO the L5 Flameling, dmg 15-21 vs HP 19-21) → Flee immediately. Else weaken via skill buttons (text `name (power)`, no testid) while opponent HP% stays above KO-guard threshold, then click recruit while own HP healthy (own card DOM); recruit_chance = 80‰ + 500‰·missingHp% (≈330‰ at half HP, ≈580‰ near 0). Each failed recruit = exactly one wild counterattack (resolve_recruit_failure); attacks also give the wild one turn. No auto battle tick — idle battles are safe. KO safety net: on SideBWins (battle row GC'd → overlay hides), heal via KeyH #heal-overlay DOM (heal_party is zone-scoped, cost 0 today — cost_currency schema gap; 30s cooldown; cap heals ≤2). Bounded: MAX_ENCOUNTERS≈14 at ≥40%/encounter → P(false-red)<1e-3; test.setTimeout per empirical runtime (≤300s hard). Success assert: ownMonsters().length===before+1 AND new monster partySlot===255 (camelCase! snapshot shape is {monsterId,speciesId,nickname,level,partySlot}; NOT the old snake_case MonsterPub interface; NOT ownedMonsters/monsterPubCount).
- R3 REVIVE (piggyback on R2's success, same describe.serial): capture battleId (ongoingBattle) at each encounter start; after R2's success the battle row SURVIVES with SideAWins (attempt_recruit GC gap, taming.rs:139-151) → assert outcome frame DOM text 'Victory!' (distinct from 'Defeat...'/'Got away safely!'); secondary cross-check `spacetime sql -s $STDB_SERVER <db> "SELECT outcome FROM battle WHERE battle_id = <id>"` .includes('SideAWins') (syntax precedent scripts/smoke-republish.sh; enum prints as variant name; no query echo). ongoingBattle snapshot CANNOT see SideAWins (Ongoing-only filter; latestPlayerBattle not exposed) — DOM is the observable. COUPLING: if R2 re-anchors, R3 re-anchors (same blocker).
- R4 RE-ANCHOR (precise real blocker, reviewer M5): bait requires grant_bait (not page-callable: browser DbConnection built without .withToken, SDK persists no token → no way to authenticate as the browser identity from the page or HTTP API) or the shop path (Lure Berry costs 200; battle-win reward ≈31/KO-win → ~7 KO battles, over the e2e time/flake budget). Revival condition: a client/src slice exposes a test-only bait-grant/startWildBattle on __game() (main.ts owned by m13.5b today). Text MUST NOT contain 'dev_reducers'/'dev-reducers'/'M12.5-recruit'.

## Detector (13.5h-2) final design
- workflowPublishesDevReducers(src): line scan, skip lines whose trimStart() starts with '#'; hit iff a line has ('--features' AND 'dev_reducers') OR ('--bin-path' AND ('MR_DEV_MODULE_WASM' OR 'dev_reducers')). indexOf only. Accepted gaps (documented in eval header): shell line-continuation token splitting; env-var rename (build-step signal still matches).
- fixmeCitesDevReducers(src): file-level (mirrors hasExpiredFixme; catches header-comment citations — the real-world pattern; per-block brace-matching rejected: fragile + the reviewer's rewording-loophole critique applies to any text matcher and re-anchoring-with-different-words is the spec's own design); hit iff contains 'test.fixme' AND ('dev_reducers' OR 'dev-reducers' — red-team F10 hyphen bypass).
- devReducerRevivalStatus({specSources, workflowSources}) → {violated, offenders, reason}; eval FAILS if violated.
- Scan scope (reviewer B4/m2): workflows = readdirSync('.github/workflows') *.yml|*.yaml ALL files; specs = client/e2e/*.spec.ts ONLY (never evals/**, so synthetic fixtures in the eval file can't self-trip).
- Teeth: W1 (--features form +), W2 (--bin-path form +), W3 (comment-only → false), W4 (stock ci.yml → false), F1 (fixme cites +), F1h (hyphen form +), F2 (re-anchored R4-style text → false), F3 (no fixme → false), S1 (publish+cite → violated, THE forcing tooth), S2 (no publish+cite → ok), S3 (publish+no cite → ok), R-real (real tree → ok).
- EXPIRED_FIXME_MILESTONES += 'M12.5-recruit' (never existed in the spec corpus — citing it is always invalid; closes the original 13.5h loophole). REQUIRES modernizing the T-E2 fixture (currently cites M12.5-recruit+dev_reducers as a "still-valid" blocker — factually expired now): re-point its fixture text at a genuinely-pending blocker with no milestone token (fixture modernization, not an assertion weakening — verifier note).

## Infra (13.5h-1, specialist)
- ci.yml e2e job: unconditional step (NO if:, NO continue-on-error — e2eGateIsBlocking rejects any if: in the block): `cargo build -p monster-realm-module --release --target wasm32-unknown-unknown --features dev_reducers` after "Pin spacetime 2.6.0"; add `MR_DEV_MODULE_WASM: ${{ github.workspace }}/target/wasm32-unknown-unknown/release/monster_realm_module.wasm` to the Two-window e2e step env. Keep `- run: node evals/ci-gate-wiring.eval.mjs` byte-exact. Artifact name verified (builds in ~11s warm in the worktree).
- client/e2e/global-setup.ts (recorded touches deviation — it IS the publish point): when MR_DEV_MODULE_WASM set → `--bin-path "<path>"` (quoted — red-team F11), else byte-identical --module-path behavior.
- ADR-0086: publish topology; HONEST no-test-consumer-yet rationale (R1-R3 revived gameplay-driven; dev publish lands per spec mandate + compiles the feature on every PR + mechanically unblocks the future __game()-hooks slice + the detector's forcing premise); strategy decision + R4 re-anchor; cache note (dual feature-set artifact, no invalidation, v1-e2e prefix untouched).
- Minimal ARCHITECTURE.md addition (e2e section).

## Verification
- just lint (biome covers client/e2e — no exclusion), node evals/spec-gap-revival.eval.mjs, node evals/e2e-desync-teeth.eval.mjs + ci-gate-wiring + cache-freshness against edited ci.yml.
- Tester-exec: ≥3 full `just e2e` green runs w/ MR_DEV_MODULE_WASM + VITE_STDB_DB=monster-realm-m135h + MR_E2E_PORT=5291; 1 run WITHOUT the var (strategy-A independence); R2 decision gate (any red or >~150s observed → re-anchor R2+R3 with the R4-style blocker).
- Full `just ci` once pre-PR. golden/zoneSync stay green (verified: their paths never touch grass).
