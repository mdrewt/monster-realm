# M8.7b — Server hardening (plan checkpoint)

Slice of `specs/monster-realm-v2/M8.7-third-review-residuals.spec.md` §4 (7b). Base:
origin/master `6187102` (M8.7a merged). Branch `feat/m8.7b-server-hardening`. ADR-0054.

## Scope (5 items + ADR)
1. Gate `start_wild_battle` (lib.rs:1483) + `grant_bait` (lib.rs:2063) behind
   `#[cfg(feature = "dev_reducers")]` (outermost attr, above `#[spacetimedb::reducer]`);
   `[features] dev_reducers = []` (NOT default) in `server-module/Cargo.toml`.
2. RETAIN `start_wild_battle` behind the gate (ADR-0054 §2 — 7e e2e needs it).
3. `start_wild_battle`: bind the caller `Character` (lib.rs:1492-1502), reject `zone_id !=
   character.zone_id` with `Err` **before** `lead_party` (reject-not-clamp).
4. Inventory single-stack mechanical via parity eval `evals/inventory-single-stack.eval.mjs`
   (every `inventory().insert(` routes through `grant_item`) — no multi-col unique in 1.12.0.
5. `content_version`: WIRE additively (server `const CONTENT_VERSION`, init seeds sentinel 0,
   `sync_content_inner` version-gates the re-seed).
6. ADR-0054 (dev-reducer release-gating + zone authz + inventory-fallback + content_version).

## Touch-set + sanctioned recorded expansions
- Declared: `server-module/src/lib.rs`, `server-module/Cargo.toml`, root `Cargo.toml`,
  `docs/adr/0054-*`, e2e CI/justfile (only IF needed — NOT needed: golden e2e doesn't call
  the dev reducers).
- Expansions (recorded, collision-safe in this single-slice run):
  - `client/src/module_bindings/` — **regenerated** (`just gen`) to drop the two gated
    reducer bindings (intrinsic generated-artifact consequence; spacetime 2.6.0 has no
    feature passthrough → default generate omits the gated reducers → bindings-drift would
    red otherwise).
  - NEW `evals/dev-reducer-gating.eval.mjs` + `evals/inventory-single-stack.eval.mjs`
    (run.mjs auto-discovers `*.eval.mjs`; no registration edit).
  - inline `#[cfg(test)]` tests in lib.rs (gated dev-reducer tests under
    `#[cfg(all(test, feature = "dev_reducers"))]`).

## Proof-of-teeth (must bite)
- Release build exposing either reducer → `dev-reducer-gating` eval fails (checks `#[cfg]`
  presence AND `dev_reducers` ∉ default).
- `zone_id != character.zone_id` → `Err` (source-scan + optional gated Rust test).
- Inventory insert outside `grant_item` → `inventory-single-stack` eval fails.
- content_version read genuinely gates re-seed (fresh init seeds; redundant sync no-ops).
- bindings-drift stays green only because `just gen` dropped the two reducer bindings.

## Key constraints / risks
- spacetime 2.6.0 `build`/`generate`/`publish` have NO `--features` passthrough → the gate
  lives in Cargo default (its absence); committed bindings = default-build bindings.
- `just test`/`typecheck` run WITHOUT `--all-features` → gated Rust tests must be
  `#[cfg(all(test, feature = "dev_reducers"))]`; the source-scan eval is the load-bearing
  zone-reject proof. `just lint` runs `--all-features` (compiles the gated code).
- `bindings-drift` skips when the CLI is absent locally → verify with CLI present; confirm
  the two `_reducer.ts` files are deleted in the diff.

## Pre-existing residuals (out of 7b scope — recorded for follow-up)
- recruit-reducer-security.eval.mjs: dead branch in `checkConsumeOneUsesCheckedSub`;
  `checkWildBattleGuard` accepts `return Ok(())` as a rejection (7a-owned eval).
- battle-schema-snapshot regex fragility (7a-owned).
- inventory "RLS by owner_identity" doc lie at lib.rs:273-274 → slice 7d.
