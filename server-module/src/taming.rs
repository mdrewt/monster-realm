//! `taming` — server-module domain submodule (M8.9, ADR-0056).
//!
//! SCAFFOLD (M8.9a): intentionally empty. M8.9b ("the move") relocates the
//! taming/inventory reducers + helpers out of `lib.rs` into here:
//!   attempt_recruit, grant_bait (reducers) + grant_item, consume_one (helpers).
//!
//! This file name is part of the canonical `touches:` vocabulary fixed by
//! ADR-0056 — keep it stable; renaming invalidates downstream `touches:` sets.
