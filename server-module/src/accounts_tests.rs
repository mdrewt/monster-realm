//! `accounts_tests` — gating tests for M21a (ADR-0179, AUTH-1..38 in-scope).
//!
//! PLACEHOLDER — the `tester` agent authors the real suite here (pure-core unit
//! tests over `accounts.rs`'s seams + source-scan mirrors of gates G2–G6/G11/G12
//! per G7). This stub only lets the crate compile before that pass lands.

#![cfg(test)]
